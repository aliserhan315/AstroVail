import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  ownedActionsContainer: {
    gap: 12
  },
  unownedActionsContainer: {
    gap: 12,
    marginTop: 8,
    marginLeft: 8,
    marginRight: 8
  }
});